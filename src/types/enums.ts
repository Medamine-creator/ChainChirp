// =============================================================================
// Runtime Enums (compiled to JavaScript)
// =============================================================================

// Output Formats
export enum OutputFormat {
  DEFAULT = 'default',
  JSON = 'json',
  TABLE = 'table',
  CSV = 'csv',
}

// Command Categories
export enum CommandCategory {
  MARKET = 'market',
  CHAIN = 'chain',
  LIGHTNING = 'lightning',
  SENTIMENT = 'sentiment',
} 